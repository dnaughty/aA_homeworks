class Stack


  attr_reader :stack
    def initialize
      @stack = []
      # create ivar to store stack here!
    end

    def push(el)
      # adds an element to the stack
      stack.push(el)
    end

    def pop
      # removes one element from the stack
      stack.pop
    end

    def peek
      # returns, but doesn't remove, the top element in the stack
      stack[-1]
    end
  end

  def Queue

    attr_reader

    def initialize

      @queue = []

    end

    def enqueue(ele)

      @queue.push(ele)

    end

    def dequeue

      @queu.shift
    end
  end



class Map

  attr_reader :map, :key, :value

  def initialize
    @map = []
  end

  def set(k)

  def get(value)












end





